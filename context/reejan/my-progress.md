# Reejan — Progress Tracker

Personal progress. Merges into `context/progress-tracker.md` via `scripts/merge-context.sh`.

## My Completed

- Dark theme tokens defined in globals.css
- UI context file created (colors, typography, layout patterns)

## My In Progress

- None yet.

## My Next Up

- Install shadcn/ui and add base components (button, card, dialog, input, table)
- Build auth pages (login, signup, OTP verification screens)
- Build student dashboard layout (sidebar + main content)
- Build dashboard components (token balance card, fee preview, progress bar)
- Build course listing page
- Build admin panel layout (tabbed: verification queue | penalties | placements)
- Build admin verification queue UI
- Build admin penalty management UI
- Build admin placement creation UI

## My Blockers

- Need API endpoints from utsav before building data-fetching components
- Need Supabase auth setup before building auth pages
